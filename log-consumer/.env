APP_ENV=dev
APP_DEBUG=true
APP_SECRET=71cd22de75da0735a2970696e5e88ca4
APP_SHARE_DIR=var/share

DEFAULT_URI=http://localhost

RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_VHOST=%2f

MESSENGER_TRANSPORT_DSN="amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@${RABBITMQ_HOST}:${RABBITMQ_PORT}/${RABBITMQ_VHOST}"

DB_NAME=log_service
DB_USER=log_service
DB_PASSWORD=log_service
DB_HOST=database
DB_PORT=5432

DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?serverVersion=16&charset=utf8"
